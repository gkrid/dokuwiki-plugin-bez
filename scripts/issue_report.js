
bez.ctl.issue_report = function() {
	bez.rich_text_editor(jQuery('textarea#description'), jQuery('.bez_description_toolbar'));
	bez.rich_text_editor(jQuery('textarea#opinion'), jQuery('.bez_opinion_toolbar'));
}
