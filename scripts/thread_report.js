
bez.ctl.thread_report = function() {
	bez.rich_text_editor(jQuery('textarea#content'), jQuery('.bez_description_toolbar'));
	//bez.rich_text_editor(jQuery('textarea#opinion'), jQuery('.bez_opinion_toolbar'));
};
